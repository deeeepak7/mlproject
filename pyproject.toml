[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mlproject"
version = "0.0.1"
dependencies = [
    "Flask",
    "pandas",
    "numpy",
    "scikit-learn",
    "catboost",
    "xgboost",
    "gunicorn"
]